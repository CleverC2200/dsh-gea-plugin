# 三栏工作台验收记录

代码基线：DSH 个人 fork `7ef3457e9d`；插件 `53e7fbe`。当前启动入口为本机 `3199`，使用 `gea-readonly-fork` profile 和 `source: "gea"`，不经过 AionUi 运行时。

| 项目              | 结果与范围                                                                                                                                                                          |
| ----------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 三栏组装          | 左侧 GEA 导航、中间独立业务文档、右侧原生 DSH `main/conversation`；桌面并排、700px 窄屏上下排列                                                                                     |
| 原审批组件        | 复制原源码及其组件依赖；5 个原 CSS 文件与来源清单的 SHA-256 完全一致                                                                                                                |
| 页面到会话        | 浏览器覆盖登录、查询、组织选择、分析范围切换、输入预览、持久化回执；提交后中间页面保持打开                                                                                          |
| 输入范围          | 汇总与完整明细明确区分；汇总保留当前版本源字段，并单独标注 SKU 数量/金额合计依据；缺失值不补造，超限明细不截断                                                                      |
| 直接 GEA 模型适配 | 受控真实 profile 覆盖凭证发现、模型发现、完整文本落盘、截断失败、长度终止、取消及 reader 释放                                                                                       |
| 插件本地检查      | `npm run typecheck`、`npm run build`、`npm test` 通过；最后全量回归 48/48，无跳过项                                                                                                 |
| DSH 检查          | build、lint、doc-sync 34 项、定向行为与原生组装快照、bundle 检查和 built-boot 通过；独立 `verify-client-domain-graph` 仍有 25 条未修改文件中的既有违规，不能称所有仓库检查通过      |
| 真实 GEA 查询     | 新工作台已读取测试环境的一条计划、一个周期和一个版本，并核对组织、数量和金额；实际计划有 205 条 SKU                                                                                 |
| 真实模型完整回答  | 尚未通过本轮最终验收。已验证真实汇总进入原生 Session；此前模型请求被旧的 15 秒本地超时中止。独立模型超时与 SSE 完整性修复已通过回归，当前重启后的进程等待重新登录并重跑真实模型验证 |

浏览器和 Session 证据保存在忽略提交的 `.runtime/web-evidence/`、`.runtime/live-evidence/` 和 `.runtime/fork-development/`。它们包含本机业务数据或启动认证信息，不随源码发布。`npm run verify -- --runtime <目录> --session <会话ID> --require-live` 可在登录后核对指定会话的当前进程、来源、完整首轮和实际模型，不能用失败或旧进程会话替代新验收。

本记录只覆盖三栏重构和只读分析路径。审批保存/通过/退回、消息待办、完整 Agent 工具、独立需求提报页面、独立安装包和生产验收仍未完成。相关 Issue 保持打开；受控接口与浏览器测试不代表这些业务能力已交付。
