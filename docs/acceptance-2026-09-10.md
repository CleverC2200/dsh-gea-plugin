# 2026-09-10 验收记录

插件版本 `0.0.1`，固定发布依赖 `@deepseek-ai/dsh@0.1.5-rc.1`。使用独立插件目录与官方 profile；未修改 dsh 源码或安装依赖。业务数据仅来自测试环境的只读查询。

2026-09-11 的直接 GEA 模型验收使用提交 `f2d2065`。登录后的同一 dsh 进程完成真实查询、快照提交、GEA Personal Model `models` 发现和 `chat/completions` SSE；`npm run verify -- --runtime .runtime/gea-dsh --session session-3dc7568c-925c-4c4f-b1e4-7bb895451dd1 --require-live` 返回 `durableSnapshotVerified: true`、`firstAnalysisCompleted: true` 和 `freshLiveVerified: true`。凭证未写入仓库，当前运行目录仍是本地忽略文件。

## 证据范围

- 模拟外部服务：构建、类型检查及 14 项产品回归通过，包含实际 Web 页面、独立 Session 磁盘读回、失败、取消与显式重试。
- 干净目录：已提交代码导出到新临时目录，`npm ci --ignore-scripts`、构建、类型检查及查询隔离测试通过；这不等同于完整默认安装流程验收。
- 真实单计划：用户在页面登录、查询并提交真实快照；AionUi 当前选定模型约 74 秒完成回答，正常持久化。独立校验 SHA-256、当前进程和来源通过；同一会话追问完成。
- 真实查询：取得当前计划详情、1 个版本以及 205 条 SKU。接口返回总数与取得条数一致；这仅描述查询覆盖，不代表模型分析覆盖。
- 真实整合分析：详情、所选版本与 3 条 SKU 子集约 125 秒完成，结束原因 `completed`；预览 prompt 与落盘 user/message 逐字一致，SHA-256 校验通过，回答明确保留 3/205 条覆盖限制。该次整合通过 Host API 驱动，不能代替全部交互式 Web 场景。
- 新进程恢复：仅重启本次验收拥有的 profile，两条真实会话在重启前已记录的事件哈希保持不变；关闭时允许追加 `session/end-seed`，不改写原始输入或回答。新 Host 的 GEA 登录状态按设计失效。
- 输入范围：205 条 SKU 的整合快照被 `SNAPSHOT_TOO_LARGE` 拒绝；明确选择 3 条 SKU 后预览为 5,749 字节，记录 `selection=subset`、`selected=3`、`total=205`。

模型首轮将金额差异归因为平均单价或结构变化，措辞偏强；追问后明确区分算术关系与未经证明的具体业务原因。SKU 子集分析使用了快照未明确的币种表述，且根据状态码和退回原因提出了未经字典确认的推测。回答质量仍有待收紧，不能把生成式解释视为已验证的业务归因。

真实模型请求未额外保存线上 wire payload，不能用持久化快照哈希替代线上请求逐字比对；受控模型回归单独验证该转换路径。凭证、内部地址、身份和真实记录只留在忽略的本地运行目录，不包含在本记录中。

## Issue 剩余项

| Issue | 已有证据 | 仍需完成 |
| --- | --- | --- |
| #1 | 独立启动、实际页面、只读查询及回执磁盘读回 | 默认干净安装全流程、固定发布版无密钥录制回放及 Web 预期 |
| #2 | 独立登录、二维码失效、401/403、旧身份响应隔离回归 | 环境切换与负向 Web 预期、对应录制回放 |
| #3 | 周期接口、查询隔离、精确值传递 | 多页、空页及乱序等完整场景和页面预期；真实样本仅一页一条 |
| #4 | 真实详情和版本、模拟历史版本归属检查 | 跨计划与版本乱序、无版本和部分返回的完整页面回归及回放 |
| #5 | 真实 SKU 查询、明确子集和超限拒绝 | 空 SKU、乱序、错误及超限的完整自动回归和录制预期 |
| #6 | dsh Host 直接复用 GEA Personal Model API；真实单计划与 SSE 分析完成 | 币种和业务归因措辞、无密钥录制回放、线上请求独立比对；同时受 #1 交付依赖约束 |
| #7 | 标准 dsh Session 已真实落盘并通过新进程 `--require-live` 校验；模拟认证失败、限流、流中断、取消、幂等及显式重试通过 | GEA 待办联动、对应持久化录制回放和完整页面状态预期 |
| #8 | 详情、版本和 SKU 查询与范围预检 | 实际模型上下文预算、长会话输入限制、全流程 UI 及整合回放 |

当前配置中的上下文容量是部署声明值，模型目录未提供可验证的容量信息。字节预检不是实际 tokenizer 或整段历史输入预算；不能据此宣告 #8 完成。上述工程回归仍可自主推进，实际容量确认需要模型服务的明确规格。全部 Issue 保持未完成，不因某一次真实调用成功关闭。
